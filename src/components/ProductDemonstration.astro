---
// Astro wrapper component for Legata Future Section
// Static structure with selective hydration for interactive workflow
export interface Props {
  class?: string;
}

const { class: className } = Astro.props;

// Import the interactive React component
import ProductDemoWorkflow from './product-demo-workflow.tsx';
import { Button } from '@/components/ui/button';
---

<section 
  id="legata"
  class={`min-h-screen legata-gradient text-white relative overflow-hidden py-16 lg:py-24 ${className || ''}`}
>
  <!-- Clean minimal background -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(71,85,105,0.03),transparent_70%)]" />

  <!-- Hero title - clean and minimal -->
  <div class="relative z-10 text-center pt-6 sm:pt-8 lg:pt-10 pb-1 sm:pb-2 lg:pb-3">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
      <h2 class="text-3xl sm:text-4xl lg:text-5xl xl:text-6xl font-light tracking-tight leading-tight text-white/95">
        <span class="font-serif italic font-medium text-red-300">Empathie</span> trifft Legal Tech
      </h2>
      <p class="text-white/60 text-base lg:text-lg mt-3 max-w-2xl mx-auto">
        Ein kostenloses Tool für alle Familien in der Schweiz: Rechtsgültige Testamente, verständlich und sicher. FADP-konform.
      </p>
    </div>
  </div>

  <!-- Interactive workflow animation - hero section with responsive sizing -->
  <div class="relative flex-1 flex items-center justify-center py-2 sm:py-6 lg:py-8 min-h-[35vh] sm:min-h-[45vh] lg:min-h-[50vh] xl:min-h-[55vh]">
    <div class="w-full max-w-7xl mx-auto px-6 lg:px-8">
      <ProductDemoWorkflow client:visible />
    </div>
  </div>

  <!-- Authentic mission-driven CTA -->
  <div class="relative z-10 text-center pb-8 sm:pb-10 lg:pb-12">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
      <p class="text-white/70 text-base lg:text-lg mb-6 max-w-2xl mx-auto leading-relaxed">
        Legata zeigt, was möglich ist: Testament-Entwürfe statt mehreren Beratungsterminen. Notariatskosten reduziert durch optimale Vorbereitung. Ähnliche Effizienzgewinne schaffen wir für Ihre Prozesse.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-3 justify-center items-center px-6">
        <a href="https://calendly.com/falx-ch/free-strategy-call" target="_blank" rel="noopener noreferrer" class="w-full sm:w-auto sm:order-1">
          <div class="relative group text-center sm:text-left">
            <Button variant="primary" className="hover-lift animate-on-scroll w-full sm:w-auto px-6 py-4 text-base">
              Was wenn Ihr Business mitdenkt?
            </Button>
            <div class="absolute top-full left-1/2 transform -translate-x-1/2 mt-2 px-3 py-1 bg-black/80 text-white text-xs rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 whitespace-nowrap pointer-events-none">
              Kostenlose Beratung
            </div>
          </div>
        </a>
        <div class="relative group w-full sm:w-auto sm:order-2 hidden lg:block">
          <Button variant="secondary" className="opacity-50 cursor-not-allowed w-full sm:w-auto px-6 py-4 text-base" disabled>
            Bei Launch benachrichtigen
          </Button>
          <div class="absolute top-full left-1/2 transform -translate-x-1/2 mt-2 px-3 py-1 bg-black/80 text-white text-xs rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 whitespace-nowrap pointer-events-none">
            Bald verfügbar
          </div>
        </div>
      </div>
      
    </div>
  </div>
</section>